<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sneaker Marketplace</title>
</head>
<body>
    <header>
        <h1>Sneaker Marketplace</h1>
    </header>
    <main>
        <section id="featuredSneakers">
            <h2>Featured Sneakers</h2>
            {{#each featuredSneakers}}
                <div class="sneakerCard">
                    <img src="{{image}}" alt="{{name}}">
                    <p>{{name}}</p>
                    <p>${{price}}</p>
                    <button>Buy Now</button>
                </div>
            {{/each}}
        </section>
    </main>
    <footer>
        <p>&copy; 2024 Holy Soley</p>
    </footer>
</body>
</html>