<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{user.username}}'s Profile - Sneaker Marketplace</title>
</head>
<body>
    <header>
        <h1>Sneaker Marketplace</h1>
    </header>
    <main>
        <section id="userProfile">
            <h2>{{user.username}}'s Profile</h2>
            <p>Email: {{user.email}}</p>
            <h3>Listed Sneakers</h3>
            {{#each user.listedSneakers}}
                <div class="sneakerCard">
                    <img src="{{image}}" alt="{{name}}">
                    <p>{{name}}</p>
                    <p>${{price}}</p>
                    <button>Edit Listing</button>
                </div>
            {{/each}}
        </section>
    </main>
    <footer>
        <p>&copy; 2024 Holy Soley</p>
    </footer>
</body>
</html>
